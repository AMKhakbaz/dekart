# راهنمای استفاده از Docker Compose Build

## مشکل اصلی چه بود؟

ورک‌فلاو قبلی شما به دلایل زیر خروجی قابل دانلود تولید نمی‌کرد:

1. **عدم آپلود آرتیفکت**: مرحله `upload-artifact` به درستی پیکربندی نشده بود
2. **عدم بررسی خطا**: اگر ساخت تصویر شکست می‌خورد، ورک‌فلاو بدون خطا ادامه می‌یافت
3. **هشدارهای Node.js 20**: اکشن‌های قدیمی نیاز به به‌روزرسانی داشتند

## تغییرات اعمال شده

### 1. به‌روزرسانی ورک‌فلاو `build-complete.yaml`

- افزودن `fetch-depth: 0` به checkout برای دسترسی به تمام تاریخچه
- افزودن `outputs: type=docker,dest=/tmp/dekart-image.tar` برای ذخیره مستقیم تصویر
- افزودن مرحله تأیید ساخت برای اطمینان از موفقیت‌آمیز بودن build
- افزودن `if-no-files-found: error` به upload-artifact برای اطمینان از آپلود موفق
- غیرفعال کردن provenance و sbom برای سرعت بخشیدن به build

### 2. بهبود فرآیند ساخت

- تصویر داکر مستقیماً به فایل tar ذخیره می‌شود
- فشرده‌سازی با gzip برای کاهش حجم
- بررسی وجود فایل قبل از آپلود

## نحوه استفاده

### روش 1: اجرای ورک‌فلاو در GitHub Actions

1. به ریپازیتوری خود در GitHub بروید
2. به تب **Actions** بروید
3. ورک‌فلاو **"Complete Docker Compose Build"** را انتخاب کنید
4. روی **Run workflow** کلیک کنید
5. تنظیمات را انتخاب کنید:
   - **target**: oss (یا premium/cloud)
   - **push_to_registry**: false (اگر می‌خواهید آرتیفکت دانلود کنید)
   - **save_artifact**: true
6. روی **Run workflow** کلیک کنید

### روش 2: دانلود و استفاده از آرتیفکت‌ها

پس از اتمام ورک‌فلاو:

1. به صفحه اجرای ورک‌فلاو بروید
2. در بخش **Artifacts**، دو فایل را دانلود کنید:
   - `dekart-docker-image-oss` (تصویر داکر فشرده)
   - `dekart-docker-compose-oss` (فایل‌های Docker Compose)

3. فایل‌ها را استخراج کنید:
```bash
unzip dekart-docker-image-oss.zip
unzip dekart-docker-compose-oss.zip
```

4. تصویر داکر را بارگذاری کنید:
```bash
gunzip dekart-oss.tar.gz
docker load -i dekart-oss.tar
```

5. به پوشه output بروید و محیط را تنظیم کنید:
```bash
cd output
cp .env.example .env
```

6. فایل `.env` را ویرایش کرده و توکن Mapbox خود را اضافه کنید:
```
DEKART_MAPBOX_TOKEN=pk.your_mapbox_token_here
```

7. سرویس‌ها را راه‌اندازی کنید:
```bash
docker compose up -d
```

8. مرورگر را باز کرده و به آدرس http://localhost:8080 بروید

## عیب‌یابی

### اگر آرتیفکت‌ها دانلود نشدند:

1. بررسی کنید که ورک‌فلاو با موفقیت به پایان رسیده باشد
2. به بخش **Artifacts** در صفحه اجرای ورک‌فلاو بروید
3. اگر آرتیفکتی وجود ندارد، لاگ‌های مراحل را بررسی کنید

### اگر تصویر داکر بارگذاری نشد:

```bash
# بررسی تصاویر موجود
docker images | grep dekart

# اگر تصویر وجود ندارد، مجدد بارگذاری کنید
docker load -i dekart-oss.tar
```

### اگر سرویس‌ها شروع نشدند:

```bash
# بررسی لاگ‌ها
docker compose logs -f dekart

# بررسی وضعیت سرویس‌ها
docker compose ps

# راه‌اندازی مجدد
docker compose restart
```

## هشدارهای Node.js 20

هشدارهایی که مشاهده کردید مربوط به منسوخ شدن Node.js 20 در GitHub Actions است. این هشدارها بر عملکرد ورک‌فلاو تأثیری ندارند، اما توصیه می‌شود در آینده اکشن‌ها را به نسخه‌های جدیدتر به‌روزرسانی کنید.

## فایل‌های تولید شده

پس از اجرای موفق ورک‌فلاو، فایل‌های زیر تولید می‌شوند:

### در آرتیفکت تصویر داکر:
- `dekart-{target}.tar.gz` - تصویر داکر فشرده

### در آرتیفکت Docker Compose:
- `docker-compose.yml` - فایل پیکربندی Docker Compose
- `.env.example` - فایل نمونه متغیرهای محیطی
- `README.md` - راهنمای استفاده

## نکات مهم

1. **توکن Mapbox**: برای استفاده از نقشه‌ها، باید توکن Mapbox معتبر داشته باشید
2. **ذخیره‌سازی داده‌ها**: داده‌ها در volume‌های داکر ذخیره می‌شوند و با حذف کانتینر از بین نمی‌روند
3. **پورت‌ها**: پورت 8080 برای Dekart و پورت 5432 برای PostgreSQL استفاده می‌شود
4. **ریست کامل**: برای پاک کردن تمام داده‌ها از `docker compose down -v` استفاده کنید
